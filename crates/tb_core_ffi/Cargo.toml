[package]
name = "tb_core_ffi"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
tb_reports = { path = "../tb_reports" }
tokscale-core = { path = "../../vendor/tokscale-core" }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
rayon = "1.10"
